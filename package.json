{
  "name": "intentproof",
  "version": "0.1.0",
  "private": true,
  "description": "Intent-driven, human-approved, evidence-backed web test authoring platform.",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.44.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "faiss-node": "^0.5.1"
  }
}
